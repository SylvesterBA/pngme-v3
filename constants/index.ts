const LABELS = {
	login: "Login",
};
const UNICODE = {
	rightArrow: '2192',
};

const UNICODE_SYMBOLS = {
	rightArrow: String.fromCodePoint(parseInt(UNICODE.rightArrow, 16)),
};

export { LABELS, UNICODE, UNICODE_SYMBOLS };
